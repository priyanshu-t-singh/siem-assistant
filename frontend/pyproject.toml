[project]
name = "frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Priyanshu T", email = "priyanshu9943@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
    "streamlit>=1.64.0",
]

[project.scripts]
frontend = "frontend:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
